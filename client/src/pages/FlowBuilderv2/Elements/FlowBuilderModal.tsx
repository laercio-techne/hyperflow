import { createPortal } from "react-dom";

const FlowBuilderModal: any = ({
  children,
  isOpen,
  className = "",
  onClose,
}: any) => {
  const rootDiv = document.querySelector("#root");
  return (
    <>
      
    </>
  );
};

export default FlowBuilderModal;
